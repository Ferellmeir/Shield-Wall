:root {
  color-scheme: light;
  --ink: #1f2420;
  --muted: #687166;
--paper: #d9e1e8;
--paper-strong: #edf3f8;
--wood: #4b5d6b;
--wood-deep: #2f3f4a;
  --iron: #495057;
  --sea: #577a95;
--sea-dark: #405f78;
  --gold: #b8a36a;
  --blood: #9f2f2f;
  --moss: #6a7f8e;
  --line: rgba(31, 36, 32, 0.18);
  --shadow: rgba(38, 27, 18, 0.18);
  font-family: "Segoe UI", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-width: 320px;
  min-height: 100vh;
  color: var(--ink);
  background:
    linear-gradient(90deg, rgba(47, 29, 23, 0.08) 0 1px, transparent 1px 80px),
    linear-gradient(180deg, #d9eee8 0%, var(--paper) 42%, #e6d4b2 100%);
}

button,
select,
input {
  font: inherit;
}

button {
  min-height: 36px;
  border: 1px solid rgba(31, 36, 32, 0.18);
  border-radius: 7px;
  padding: 8px 12px;
  color: #fff;
  background: var(--wood);
  cursor: pointer;
  transition: transform 120ms ease, filter 120ms ease, border-color 120ms ease;
}

button:hover:not(:disabled) {
  transform: translateY(-1px);
  filter: brightness(1.05);
}

button:disabled {
  cursor: not-allowed;
  opacity: 0.45;
}

select,
input[type="number"] {
  min-height: 34px;
  border: 1px solid var(--line);
  border-radius: 6px;
  padding: 7px 9px;
  background: var(--paper-strong);
  color: var(--ink);
}

.app-shell {
  min-height: 100vh;
}

.game {
  display: grid;
  grid-template-columns: 230px minmax(0, 1fr);
  min-height: 100vh;
}

.side {
  position: sticky;
  top: 0;
  height: 100vh;
  padding: 18px 14px;
  color: #f8efd4;
  background:
    linear-gradient(180deg, rgba(47, 29, 23, 0.92), rgba(91, 53, 34, 0.94)),
    repeating-linear-gradient(90deg, rgba(255, 249, 232, 0.86) 0 2px, transparent 2px 28px);
  box-shadow: 6px 0 22px var(--shadow);
}

.brand {
  margin: 0 0 14px;
  padding-bottom: 14px;
  border-bottom: 1px solid rgba(248, 239, 212, 0.22);
}

.brand h1 {
  margin: 0;
  font-size: 28px;
  letter-spacing: 0;
}

.brand p {
  margin: 5px 0 0;
  color: rgba(248, 239, 212, 0.72);
  line-height: 1.35;
}

.nav {
  display: grid;
  gap: 8px;
}

.nav button {
  width: 100%;
  justify-content: flex-start;
  text-align: left;
  color: #f8efd4;
  background: transparent;
  border-color: rgba(248, 239, 212, 0.18);
}

.nav button.active {
  color: #2b1a12;
  background: #f2d27c;
  border-color: #f2d27c;
}

.save-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
  margin-top: 16px;
}

.save-row button {
  padding-inline: 8px;
  color: #f8efd4;
  background: rgba(255, 255, 255, 0.08);
}

.content {
  min-width: 0;
  padding: 18px;
}

.topbar {
  display: grid;
  grid-template-columns: repeat(6, minmax(104px, 1fr));
  gap: 10px;
  margin-bottom: 18px;
}

.stat-tile {
  min-height: 70px;
  padding: 12px;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: rgba(255, 249, 232, 0.78);
  box-shadow: 0 8px 24px rgba(67, 48, 28, 0.09);
}

.stat-tile span {
  display: block;
  color: var(--muted);
  font-size: 12px;
  text-transform: uppercase;
}

.stat-tile strong {
  display: block;
  margin-top: 4px;
  font-size: 22px;
}

.panel {
  border: 1px solid var(--line);
  border-radius: 8px;
  background: rgba(255, 249, 232, 0.86);
  box-shadow: 0 12px 32px rgba(67, 48, 28, 0.1);
  overflow: hidden;
}

.panel + .panel {
  margin-top: 14px;
}

.panel-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 14px 16px;
  border-bottom: 1px solid var(--line);
  background: rgba(245, 229, 186, 0.66);
}

.panel-head h2,
.panel-head h3 {
  margin: 0;
  font-size: 19px;
}

.panel-body {
  padding: 16px;
}

.grid {
  display: grid;
  gap: 12px;
}

.grid.two {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.grid.three {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.grid.four {
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.card {
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 13px;
  background: rgba(255, 255, 255, 0.45);
}

.card h3,
.card h4 {
  margin: 0 0 8px;
}

.muted {
  color: var(--muted);
}

.tiny {
  font-size: 12px;
}

.actions {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
}

.secondary {
  color: var(--ink);
  background: #e9d49e;
}

.danger {
  background: var(--blood);
}

.sea {
  background: var(--sea-dark);
}

.moss {
  background: var(--moss);
}

.gold {
  color: #2e2110;
  background: #d7ae45;
}

.table {
  width: 100%;
  border-collapse: collapse;
}

.table th,
.table td {
  padding: 9px 8px;
  border-bottom: 1px solid var(--line);
  text-align: left;
  vertical-align: top;
}

.table th {
  color: var(--muted);
  font-size: 12px;
  text-transform: uppercase;
}

.meter {
  width: 100%;
  height: 8px;
  border-radius: 999px;
  background: rgba(31, 36, 32, 0.14);
  overflow: hidden;
}

.meter span {
  display: block;
  height: 100%;
  background: var(--sea);
}

.meter.danger span {
  background: var(--blood);
}

.meter.gold span {
  background: var(--gold);
}

.badge-row {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.badge {
  display: inline-flex;
  align-items: center;
  min-height: 24px;
  padding: 3px 7px;
  border: 1px solid rgba(31, 36, 32, 0.14);
  border-radius: 999px;
  color: var(--ink);
  background: rgba(255, 255, 255, 0.5);
  font-size: 12px;
}

.log {
  display: grid;
  gap: 8px;
  max-height: 62vh;
  overflow: auto;
  padding-right: 4px;
}

.log-entry {
  padding: 10px;
  border-left: 4px solid var(--sea);
  background: rgba(255, 255, 255, 0.48);
}

.battle-layout {
  display: grid;
  grid-template-columns: minmax(0, 1.3fr) minmax(320px, 0.7fr);
  gap: 14px;
}

.battle-canvas-wrap {
  min-height: 420px;
  border: 1px solid var(--line);
  border-radius: 8px;
  overflow: hidden;
  background: #bdd8cb;
}

#battleCanvas {
  display: block;
  width: 100%;
  height: 420px;
}

.battle-message {
  min-height: 70px;
  margin-top: 10px;
  padding: 12px;
  border-radius: 8px;
  color: #f7edd1;
  background: var(--wood-deep);
}

.fighter-list {
  display: grid;
  gap: 8px;
  max-height: 420px;
  overflow: auto;
}

.fighter {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 8px;
  align-items: center;
  padding: 10px;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.5);
}

.fighter strong {
  display: block;
}

.fighter select {
  max-width: 150px;
}

.start-screen {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 20px;
  background:
    linear-gradient(rgba(31, 36, 32, 0.14), rgba(47, 29, 23, 0.24)),
    linear-gradient(140deg, #e7d4a8 0%, #eaf3ed 42%, #9dc0b2 100%);
}

.start-panel {
  width: min(960px, 100%);
  border: 1px solid rgba(31, 36, 32, 0.2);
  border-radius: 8px;
  background: rgba(255, 249, 232, 0.9);
  box-shadow: 0 26px 70px rgba(47, 29, 23, 0.22);
  overflow: hidden;
}

.start-hero {
  display: grid;
  grid-template-columns: 1fr 340px;
  gap: 0;
}

.start-copy {
  padding: 28px;
}

.start-copy h1 {
  margin: 0 0 12px;
  font-size: clamp(36px, 7vw, 66px);
  line-height: 0.95;
  letter-spacing: 0;
}

.start-copy p {
  max-width: 650px;
  color: #4d4a3f;
  line-height: 1.55;
}

.leader-card {
  padding: 22px;
  border-left: 1px solid var(--line);
  background: rgba(242, 210, 124, 0.24);
}

.leader-card h2 {
  margin: 0 0 10px;
}

.stat-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 6px;
}

.stat-pill {
  display: flex;
  justify-content: space-between;
  gap: 8px;
  padding: 6px 8px;
  border: 1px solid var(--line);
  border-radius: 6px;
  background: rgba(255, 255, 255, 0.45);
  font-size: 12px;
}

.toast {
  position: fixed;
  right: 18px;
  bottom: 18px;
  z-index: 20;
  max-width: min(420px, calc(100vw - 36px));
  padding: 12px 14px;
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 8px;
  color: #fff7df;
  background: rgba(47, 29, 23, 0.95);
  box-shadow: 0 18px 40px rgba(47, 29, 23, 0.28);
}

@media (max-width: 980px) {
  .game {
    grid-template-columns: 1fr;
  }

  .side {
    position: static;
    height: auto;
  }

  .nav,
  .save-row {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .topbar {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .grid.three,
  .grid.four,
  .battle-layout,
  .start-hero {
    grid-template-columns: 1fr;
  }

  .leader-card {
    border-left: 0;
    border-top: 1px solid var(--line);
  }
}

@media (max-width: 620px) {
  .content {
    padding: 12px;
  }

  .nav,
  .save-row,
  .topbar,
  .grid.two {
    grid-template-columns: 1fr;
  }

  .table {
    display: block;
    overflow-x: auto;
  }

  .panel-head {
    align-items: flex-start;
    flex-direction: column;
  }

  .start-copy {
    padding: 22px;
  }
}
.actions {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
}

.table .actions {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

td select[data-action="training"] {
  display: block;
  width: 100%;
  margin-bottom: 4px;
}

.stat-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 4px;
}
